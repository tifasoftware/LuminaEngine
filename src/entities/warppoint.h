// warppoint.h and warppoint.cpp is deprecated

#pragma	once
/*
#include <cstring>
#include <entities/trigger.h>

class WarpPoint : public Trigger {
    public:
    WarpPoint() { }
    void setMapName(const char* map) { strcpy(newmap, map); }
    const char* GetNewMap() { return newmap; }

    static WarpPoint* createWarpPoint(const char* newmap);

    EntityType getType() override { return TRIGGER_WARP; }

    ~WarpPoint() {}

    private:
    char newmap[128] = "scrolltest.lmap";
};*/
